// Candidate-related API routes will be added here.

const express = require("express");
const router = express.Router();

// TODO: Add candidate endpoints.
// Example:
// GET /api/candidates
// GET /api/candidates/:id

module.exports = router;
